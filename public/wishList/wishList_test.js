'use strict';

describe('myApp.wishList module', function() {

  beforeEach(module('myApp.wishList'));

  describe('WishList controller', function(){

    it('should ....', inject(function($controller) {
      //spec body
      var view1Ctrl = $controller('WishListCtrl');
      expect(view1Ctrl).toBeDefined();
    }));

  });
});